apiVersion: v1
kind: Service
metadata:
  name: covid-dashboard-service
spec:
  type: LoadBalancer
  loadBalancerSourceRanges:
  - "0.0.0.0/0"
  selector:
    app: covid-dashboard
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
